[tools]
chezmoi = "latest"

[tasks.pre-commit]
run = "prek"

[tasks.bump]
description = "Upgrade All"
depends = "bump:*"

[tasks."bump:gh"]
description = "Sync Github Extensions"
run = "gh extension upgrade --all"

[tasks."bump:docker"]
description = "Sync Docker Images"
run = [
  "docker compose --file ~/.config/services/compose.yaml pull",
  "docker compose --file ~/.config/services/compose.yaml up --detach --force-recreate"
]
